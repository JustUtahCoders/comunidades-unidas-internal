language: node_js
node_js:
  - "node"
script: yarn build
before_deploy: 
  - pip install awsebcli --user
deploy:
  provider: script
  script:
    - git status
    - git checkout master
    - git status
    - ls -al
    - ebcli deploy --staged
  on:
    branch: master