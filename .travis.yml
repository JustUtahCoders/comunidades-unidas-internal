language: node_js
node_js:
  - "node"
script: yarn build
before_deploy: 
  - pip install awsebcli --user
  - ls -al
deploy:
  provider: script
  before_script:
  script: echo "Deploying with ebcli" && eb deploy test-env --profile default
  on:
    branch: master
  skip_cleanup: true