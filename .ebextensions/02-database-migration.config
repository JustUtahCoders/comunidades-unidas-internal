option_settings:
container_commands:
  database_migration:
    leader_only: true
    command: curl --silent --location https://rpm.nodesource.com/setup_14.x | bash - && yum install -y nodejs && npm config set scripts-prepend-node-path auto && echo "Running db migrations" && npm run db-migrate-prod